<div id="survey_headers">
  <div id="new-question-div"></div>
  <% if current_user %>
  <h3>Surveys that you can answer</h3>
  <% survey.each do |surv| %>
    <div class="survey">
      <a href="/survey/<%= surv.id %>"><%= surv.title %> </a>
    <% end %>
    <% else %>
      <h3>Welcome, please sign in to start</h3>

    </div>
  <% end %>
</div>
